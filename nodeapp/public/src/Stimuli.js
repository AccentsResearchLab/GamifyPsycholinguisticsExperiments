adjustGain = function(event) {
	debug("User adjusted the gain to: "+event.target.value);
};
playAudio = function(event){
	debug("User clicked play.");
};

playSoundCheckAudio = function(event){
	debug("User clicked play sound check.");
};